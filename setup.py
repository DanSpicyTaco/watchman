from setuptools import setup, find_packages

setup(
    name='watchman',
    version='0.0',
    description='Securing drone communication with an encrypted channel',
    author='DanSpicyTaco',
    packages=find_packages()
)
